---
driver:
  name: docker
  #volume: ['/var/run/docker.sock:/var/run/docker.sock']
  #privileged: true
  forward: 80
  use_sudo: false
 
provisioner:
  name: chef_solo
 
verifier:
  name: inspec
  sudo: true

platforms:
  - name: ubuntu-16.04
 
suites:
  - name: default
    run_list:
      - recipe[hello_webpage_chef]
    attributes:
